# Experiments

핸즈온 실험 코드. 각 디렉토리는 독립 실험.

## 실행 방법

```bash
cp ../../.env.example ../../.env  # 루트에서 한 번만
cd <experiment-dir>
pip install -r requirements.txt
python main.py
```

## 실험 목록

| 디렉토리 | 주제 | 상태 |
|---------|------|------|
| `agent-eval/` | LLM-as-judge 기반 에이전트 응답 평가 | 진행중 |
